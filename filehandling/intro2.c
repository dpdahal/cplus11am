#include<stdio.h>
#include<stdlib.h>

int main(){
  

    // FILE *fp;
    // char name[20];
    // fp = fopen("record.txt","w");

    // printf("Enter name:");
    // gets(name);
    // fprintf(fp,"%s",name);
    // fclose(fp);   

    // fp = fopen("record.txt","r");
    // fscanf(fp,"%s",&name);
    // printf("%s",name);
    // fclose(fp);   

    // remove("record.txt");


}